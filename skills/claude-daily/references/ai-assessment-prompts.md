# AI 使用评估契约（ai-assessment mode）

供 `claude-daily ai-assessment` 流程使用。
评估的核心问题（PRD §2）：**是人领导 AI，还是 AI 领导人。**

## 输出哲学（最重要的一条）

**判断在前、证据下钻；维度不打数字分；人级给档位但受证据门控。**（2026-07-02 v2，用户拍板）

- 维度层仍然**不打数字分**：per-dimension `0-3` 是 AI 摇摆的假精度，判断一律用「强 / 到位 / 有缺口 / 证据不足」并紧绑证据。
- 人级**恢复 PRD §8 的五档**（L0 被 AI 牵着走 / L1 会操作 AI / L2 合格协作者 / L3 强主导者 / L4 团队放大器）——负责人评估能力必须能说出"这人大致什么水平"。门控规则：
  - 高置信实质 session <3 个 → 写「样本不足，无法定档」，绝不硬定。
  - 定档必须附一句依据（据哪些行为定档）+ 明确说"单期档位是快照不是定论"。
  - 档位来自行为证据对照 §8 各档描述，不来自维度计数或平均。
- **不排名**：档位用于单人判断与培训分层，不生成团队名次表；聚合只做分布（"团队 60% 在 L2"）。
- 真正的区分力来自行为证据（loops、claim↔验证、实测反查、护栏取舍），档位只是证据的收口。

> §7 的 8 个维度名仍用；§9.3 红线规则驱动「旗标」；§9.1 的 0-3 维度分仍然弃用。

## Contents

- [通用铁律](#通用铁律)
- [§A 输入契约](#a-输入契约)
- [§B 单 session MAP（执行叙事优先）](#b-单-session-map执行叙事优先)
- [§C 个人 REDUCE（旗标 + 主题，不评级不平均）](#c-个人-reduce旗标--主题不评级不平均)
- [§D 三输出文件契约](#d-三输出文件契约)
- [§E 定性判据](#e-定性判据)

## 通用铁律

1. **证据优先**（PRD §6.1）：每个判断绑 transcript 证据；无证据写「证据不足」。
2. **行为评估，非人身**（§6.2）：写「未发现验证命令」，不写「这个人不认真」。
3. **不唯结果**（§6.4）：复杂任务失败但设了边界 / 验证 / 止损 → 仍可能是强协作；简单任务成功但全程裸奔 → 不算强。
4. **不拿 AI 自述当事实**：以 `commands[].outcome`、`claims[].prev_verify_outcome` 为准，不信 AI 嘴上「完成了」。
5. **维度不打数字分**：维度判断一律用「强 / 到位 / 有缺口 / 证据不足」并紧绑证据；人级档位按输出哲学的门控规则给。
6. **受众规则（manager 报告硬约束）**：正文零术语、零 session 哈希号、零文件路径/命令名/工具名——这些只能存在于锚点下钻。想象读者是没看过任何 session、也不写代码的负责人；「s2c/subagent/grep/stdin」这类词出现即违规，必须翻成大白话（如「学习文档生成器」「让 AI 抽查核对」）。
7. **结论优先**：每一层都判断在前、证据在后。读者 30 秒内必须能拿走"这人行不行 + 最需要过问什么"；证据是点开才看的下钻，不是正文主体。
8. **旗标一致性**：旗标必须与正文风险内容情绪一致——正文里有值得负责人过问的事，旗标就不能是「本期无明显风险」。

## §A 输入契约

只读两份，**AI 回复正文一律不读**（PRD §18.2）：

- `_assessment_context.json`（投影层产物），每 session：`commands[{cmd,kind,outcome}]`、`edit_events[{file,n}]`（不去重）、`claims[{claim,prev_verify,prev_verify_outcome}]`、`loops{cmd:连失次数}`、`danger_ops[{cmd}]`、`event_backbone[{i,type,text}]`（全量，定位锚点用）、`user_prompts`。
- `_context.json`：取 `compact_events` 还原叙事（可选）。

证据类型 → 维度绑定（防止拿 A 类证据糊 B 维度）：

| 维度 | 主证据 |
|---|---|
| 目标与上下文 | `user_prompts`（首条/每轮开头）|
| 边界与约束 | `user_prompts` 禁止项 + `danger_ops`/越界编辑 |
| 任务拆解 | `user_prompts` 阶段性 + 是否先调研再做 |
| 主导权 | `event_backbone` 里 user 掰方向/拍板 + 中断 |
| 验证意识 | `commands`(verify 的 outcome) + `claims[].prev_verify_outcome` |
| 纠错与止损 | `loops` + claim 失败后走向 + user 喊停 |
| 风险意识 | `user_prompts` 对数据/安全/删除的要求 + `danger_ops` 有无确认 |
| 沉淀复用 | `edit_events` 是否写入规则/测试/checklist 文件 |

## §B 单 session MAP（执行叙事优先）

每个 top-level session 产一份记录，三段 + 置信度。

### B.1 执行叙事（逐回合，这是 body，不是总结）

用 `event_backbone` + `user_prompts` 还原**任务怎么被一步步驱动的**。每个实质回合一条：

```
[@锚点] 用户这轮要什么（引原话）→ AI 怎么回 / 做了什么 → 这轮落地了什么（命令成败 / 编辑 / 验证）→ 命名的 pushback（如有）
```

- 覆盖主要回合，**不要压成三五条干 bullet**；标出弧线里的关键转折（掰方向 / 中断 / 撞坑 / 止损 / 截图反查）。
- 一个有 10+ 回合的 session 不应只剩 4 条。这一段展示的是过程轨迹，不是结论。
- 薄 session（prompts 少、无工具证据）：叙事一两句，写明「只读问答 / 闲聊，无可评估的协作行为」。

### B.2 缺陷根因追踪（内嵌叙事，触发即追）

触发（任一）：`claims[].prev_verify_outcome=="fail"` / `loops` 非空 / `danger_ops` 非空。
六段（PRD §18.5）：缺陷 → 怎么暴露 `[@锚点]` → 沟通根因（缺了目标/边界/验证/止损中哪项）→ 行为归因（非人身，归到委派结构缺口）→ 能力信号（缺口 + 恢复都写）→ 下次护栏（可照抄）。
无则一句 `本 session 未发现会上露头的缺陷`。

### B.3 维度判断（8 维，定性 + 证据，无数字）

8 维（PRD §7）逐个给，**不打分**：

```
<维度>：<强 | 到位 | 有缺口 | 证据不足>
- 直属证据：<引 user 原话 / 命令成败 / claims / edit_events，标 🔺最强 或 🔻最弱>
```

8 维：`目标与上下文`、`边界与约束`、`任务拆解`、`主导权`、`验证意识`、`纠错与止损`、`风险意识`、`沉淀复用`。
按 §E 判据。关键维度（主导/验证/纠错/风险）必须有**行为证据**，不凭 prompt；无证据写「证据不足」，不硬判。

### B.4 置信度

本 session 整体 `低 / 中 / 高` + 一句依据。

## §C 个人 REDUCE（v2：结论优先 + 档位 + 瞬间 + 诊断段）

跨该人本批所有 session 聚合。产出以下七件（全部遵守铁律 6-8：零术语、结论优先、旗标一致）：

- **30 秒结论**（3-5 句）：这个人是在指挥 AI 还是被 AI 带着走（绑最强证据）；一件最需要负责人过问的事（如有）；一个最大的提升空间。写完自测：不懂技术的负责人读完能不能复述出这三点。
- **档位**（按输出哲学门控）：PRD §8 五档之一 + 一句定档依据 + 大白话档名（如 L2 可写「合格协作者——会驾驭，未沉淀」）；样本不足写「无法定档」。
- **触发旗标**（可多选）：`红线命中` / `证据不足-需人工复核` / `本期无明显风险`。受铁律 8 约束：正文有值得过问的事时不得用「本期无明显风险」，此时旗标用 `红线命中` 或在 meta 的 `flag_label` 里写更贴切的黄色标签（如「有一处值得过问」）。
- **三个瞬间**（默认 1 高光 + 1 隐患 + 1 模式；无隐患可 2 高光 1 模式）：每个瞬间 = 标题（一句话说清发生了什么）+ 2-3 句白话正文（发生了什么 → 为什么说明问题）+ 用户原话引文（如有，逐字）+ session 归属 + 锚点。选取标准：优先选**改变判断**的时刻（戳穿 AI / 拦截风险 / 危险放权 / 反复出现的习惯），不选平淡的正常操作。
- **8 维诊断段**：8 个维度（§7）逐个给「判断 + 诊断段」。诊断段 2-4 句，必须含三层：**观察到什么模式**（跨 session 归纳，不是复读单件事）→ **这意味着什么**（对结果/团队的影响）→ **风险或改法**（没有就写强在哪、怎么保持）。像顾问解读，不是填表陈述；正文零术语零 session 号，证据 session+锚点缀在段尾供下钻。「证据不足」的维度也要写一句为什么不足、下期怎么才能评得出来。
- **处方**（只改一件事 + 次选）：从风险/诊断推出的最高杠杆动作，写成负责人可直接转述给本人的话。
- **置信度 + 不应过度解读**：样本薄 / `user_prompts` 多空 → 标低置信、说清哪些结论不可外推。

**红线**（命中即打 `红线命中` 旗标，PRD §9.3）：高危改动无验证/确认；架构·安全·产品关键取舍交给 AI；AI 多次跑偏无止损；生成内容进项目无 diff/test/review。

**看模式不看单次**：一个风险只在 1 个 session 出现 → 标「单次，可能任务差异」；多 session 复现 → 「稳定模式」。瞬间与诊断段引用时须带此标注。

## §D 三输出文件契约

### D.1 `_ai_assessment.personal.md`（形态 B，本人看，叙事优先）

```markdown
# 你的 AI 使用复盘 · <样本=N session>

## 一、各 session 执行叙事
<每个 session：B.1 执行叙事 + B.2 内嵌缺陷根因 + B.3 维度判断（定性）>

## 二、做得好的（保持）
- <绑具体动作 + 效果，引 user 原话>

## 三、容易埋坑的（改这里）
- <绑证据 + 为什么伤结果>

## 四、下次可以这样问（照抄，基于本人原话改写）

## 五、下次必须验证什么

## 六、可复用 checklist
```

深引、多引原话。**全文无数字分、无等级。**

### D.2 `_ai_assessment.manager.md`（形态 A v2，负责人看：结论优先、零术语、证据下钻）

```markdown
# <成员> 的 AI 使用评估
<日期范围> · <N> 个会话（其中 <M> 个有实质工作）· 评估可信度：<高/中/低>

旗标：<...>   档位：<大白话档名>（<L0-L4> · 依据一句话）

## 30 秒结论
<3-5 句，见 §C；负责人不读别的也能拿走判断>

## 三个说明问题的瞬间
### <🏆/⚠️/💡> <一句话标题>
<2-3 句白话正文>
> 用户原话：「...」（如有，逐字）
（session <short> @锚点）

## 八个能力面（判断在前，证据点开看）
**<维度名>：<强/到位/有缺口/证据不足>**
<诊断段 2-4 句：观察到什么模式 → 意味着什么 → 风险或改法>（session <short> @锚点）

## 如果只让他改一件事
<处方：首选 + 次选>

## 不应过度解读的部分
```

正文（标题、结论、瞬间正文、诊断段）执行铁律 6：零术语、零 session 号、零路径/命令名——session 归属与锚点只出现在段尾括号里（网页按 `@(\d+)` 解析并定位到该 session 的 `event_backbone` 渲染可点击弹窗，v2 骨架含 user 全文 / AI 回复 / 命令输出，可还原完整对话）。用户原话引文必须逐字。

### D.3 `_ai_assessment.meta.json`（结构化，供阶段四聚合 + §11.3 复核）

```json
{
  "member_id": "wanhua.gu",
  "date_range": "2026-06-24",
  "session_count": 5,
  "sessions": [
    {"session_short": "a8b416f0", "project": "vibejet",
     "dims": {"目标与上下文": "强", "边界与约束": "有缺口", "任务拆解": "强",
               "主导权": "强", "验证意识": "强", "纠错与止损": "到位",
               "风险意识": "到位", "沉淀复用": "到位"},
     "flags": [], "confidence": "高",
     "defects": [{"what": "...", "anchor": "@317/@325",
       "comm_root_cause": "...", "recovery": "...", "guardrail": "..."}]}
  ],
  "person": {
    "verdict_30s": "这个人明显是在指挥 AI…（30 秒结论全文，3-5 句）",
    "level": {"code": "L2", "name": "合格协作者——会驾驭，未沉淀", "basis": "定档依据一句话；样本不足时 code=null, name=\"无法定档\""},
    "flags": ["红线命中" | "证据不足-需人工复核" | "本期无明显风险"],
    "flag_label": "有一处值得过问（可选：比三值旗标更贴切的显示标签 + tone: red/yellow/green/gray）",
    "flag_tone": "yellow",
    "moments": [
      {"kind": "highlight|risk|pattern", "title": "一句话标题",
       "body": "2-3 句白话正文", "quote": "用户原话（逐字，可空）",
       "session_short": "121c44d7", "anchors": [62, 72],
       "recurrence": "单次|稳定模式"}
    ],
    "dim_diagnosis": [
      {"dim": "验证意识", "judge": "强|到位|有缺口|证据不足",
       "diagnosis": "2-4 句诊断段（观察→意味着什么→风险或改法）",
       "evidence": [{"session_short": "121c44d7", "anchors": [62]}]}
    ],
    "prescription": {"primary": "只改一件事", "secondary": "次选"},
    "judgment": "（保留，兼容旧字段）",
    "strength_themes": ["..."], "risk_themes": ["..."], "training_themes": ["..."],
    "confidence": "中：…",
    "do_not_overinterpret": "…"
  }
}
```

**维度无数字分。** `dims` 用定性词（机器仍可聚合分布：如「团队 60% 验证意识=有缺口」）；`level` 是人级档位（见输出哲学门控）。前端详情页优先从 `person` 的结构化字段（verdict_30s/level/moments/dim_diagnosis/prescription）渲染，`manager_md` 作为完整文本兜底。

## §E 定性判据

取代行为锚点打分。4 个关键维度各给「什么样算 强 / 到位 / 有缺口 / 证据不足」：

**主导权**
- 强：关键处掰方向有据（质疑方案 / 要求回到代码事实 / 动手前喊停对齐），有中断或据实拍板。
- 到位：基本保持主导，偶有照单全收。
- 有缺口：架构 / 依赖 / 范围等关键取舍多由 AI 默认决定，少追问。
- 证据不足：无足够交互可判（如只读问答）。

**验证意识**
- 强：完成主张前紧挨真实 test/build/lint 且 outcome 可查，失败如实报。
- 到位：有验证但不稳定 / 有遗漏。
- 有缺口：AI 说完成但无对应 verify，或新依赖 / 高危改动无核查。
- 证据不足：本 session 无 claim 也无可评 verify 行为。

**纠错与止损**
- 强：AI 跑偏 / 失败时早抓快止，`loops` 为空或失败后定向排查再改。
- 到位：能纠错，但出现过一次收敛慢。
- 有缺口：`loops` 非空（连失 ≥2 补丁套环）或同区域反复 edit 无收敛。
- 证据不足：本 session 无失败 / 越界事件可评。

**风险意识**
- 强：对数据 / 安全 / 删除 / 迁移设确认点，高危前要求核查。
- 到位：操作层风险可控，但某类风险（如安全含义）未显式处理。
- 有缺口：高危任务无 review / 测试 / 备份，或危险操作直接执行无确认。
- 证据不足：本 session 无涉及风险的动作。
